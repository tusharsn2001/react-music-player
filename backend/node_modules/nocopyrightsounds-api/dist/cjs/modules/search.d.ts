import { Genre, Mood } from '../helpers/enums'
import type Song from '../api/Song'
export interface Filter {
    genre?: Genre
    mood?: Mood
    search?: string
    version?: 'regular' | 'instrumental' | 'both'
}
export declare const search: (filter: Filter, page?: number) => Promise<Song[]>
